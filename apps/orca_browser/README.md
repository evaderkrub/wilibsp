# Orca Browser
LVGL story browser for FREE-WILi 2. Build with `python tools/fw.py build orca_browser`, flash with `python tools/fw.py flash orca_browser`, and inspect diagnostics with `python tools/fw.py rtt`.
